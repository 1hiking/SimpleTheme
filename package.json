{
  "name": "simpletheme",
  "license": "MIT",
  "scripts": {
    "dev": "hugo --serve --minify",
    "build": "hugo --cleanDestinationDir --minify"
  },
  "browserlist": [
    "last 4 versions",
    ">50%",
    "not dead"
  ],
  "dependencies": {
    "@fullhuman/postcss-purgecss": "^4.1.3",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.4",
    "postcss-cli": "^9.1.0"
  }
}
